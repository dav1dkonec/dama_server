using Avalonia.Controls;

namespace dama_klient_app.Views;

// Zobrazuje login formulář (Nick + tlačítko), logika je ve LoginViewModel.
public partial class LoginView : UserControl
{
    public LoginView()
    {
        InitializeComponent();
    }
}
